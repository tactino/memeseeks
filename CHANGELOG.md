# 更新说明

## 0.3.0（2026-09-26）

从「能搜的梗图库」变成个人梗图收藏夹。

**收藏与浏览**
- 图集：全部、我喜欢和自建图集；新建、重命名、删除，加入和移出梗图；梗图页显示它在哪些图集里。
- 刷梗：全屏一张接一张。从图集进入按图集顺序或随机；从某张梗图进入先刷和它相似的；从页头进入先刷最久没看过的。图集会记住你刷到哪了。
- 梗图页：永久编号、出处、所在图集，以及「相似的梗」（只在真的相似时出现）；首页有今日一梗。
- 上传：图集页的「上传」按钮，或者把图片拖进页面；手机上会调起相册。支持 HEIC。

**外观**
- 新设计：Pop 猫标志、品牌黄、暖纸色加蒙德里安边框；夜间主题。
- 动效：开场动画（每次打开播放一次）；翻页像幻灯片，卡片会飞进梗图页；小猫彩蛋（点猫、POP ×n、下拉刷新、加载动画）。「减少动效」会关掉这些。
- 设置页：主题、边框、繁体字（默认转成简体）、开场动画、动效、网上搜索、来源文件夹、连接浏览器；图库里的 `custom.css` 会在应用样式之后加载。

**采集**
- 采集脚本 0.3.0：可以拖动的猫头，按网站记住位置；「放进」可选图集；采到的图逐张飞进猫嘴；迷因捕手没在运行时显示红色提示。
- 把网页上的图片直接拖到猫头上，它就吞下去（放进上次选的图集，缩略图会换成原图）。
- 待确认：采集来的字很少的图（多半是表情包）先等你决定要不要。

**搜索与文字**
- 只看梗图自己的字：去掉平台水印（小红书号等）、截图里的账号、时间、点赞转发数、分享按钮和识别出的乱码，并把被折成几行的句子接回去。梗图页和今日一梗显示这些字，搜索和「相似的梗」也用它：不再因为同一个博主的水印把不同的梗算成相似，高把握结果里的错图少了约四分之一。

- 有显卡时可以让视觉语言模型整理文字（`memeseeks add --tidy`，Qwen3-VL-8B，约 19 GB 显存）：纠正识别错字，对话一句一行写成「- …」，评论写成「【评论】名字：…」，有中文翻译时去掉外文原文。梗图页显示整理后的文字；搜索仍用规则版，因为在标注的查询上它略好一点。
- 同一个模型还会认出知名的梗：梗图页显示「梗：电车难题」，点一下搜同一个梗的其他图，梗名本身也能搜到。外文梗图没有中文翻译时，在文字下面显示「【译】…」。
- 电脑没有显卡时，`memeseeks tidy-remote --host 主机 --home 目录` 通过 ssh 让另一台有显卡的电脑来整理；之后每次更新图库都会把没整理过的新图送过去，结果回来后那边的图就删掉。那台电脑关着也不影响图库更新，下次再送。

**手机**
- 设置里的「手机访问」：打开后电脑上显示二维码，手机扫码就能用（同一个 Wi-Fi，带口令，可以换口令）。

**安装与首次启动**
- Windows、macOS、Linux 一行命令安装：自带 Python、装进一个文件夹、建快捷方式；可以整个不放 C 盘，也能复用已有的模型；在国内自动换镜像。
- 服务器立刻可用，模型在后台加载，页头显示下载和建索引的进度；首次下载从约 8 GB 降到约 3.9 GB。

**安全**
- 其他网站不能把迷因捕手嵌进框架，也不能运行采集脚本拿到收件箱密钥；改动类请求只接受 JSON 或图片请求体。
